pipeline:

  build:
    image: hseeberger/scala-sbt
    commands:
      - sbt clean update test
    when:
      event: push

services:
  dind:
    image: docker:1.11-dind
    privileged: true
    command:
      - "-s"
      - "overlay"